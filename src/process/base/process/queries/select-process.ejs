<%

cms.utils.trace("cms.process.base.process.queries.selectProcess");
const table = sqlString.escapeId(processInstance.table)
const escapedId = sqlString.escape(id);
const tableFieldsBrute = cms.schema.constraints[processInstance.table].attributes.map(attr => processInstance.table + "." + attr);
const tableFields = cms.utils.toSelectFieldsSql(0, tableFieldsBrute);

%>
SELECT <%-tableFields%>
  FROM <%-table%>
  WHERE <%-table%>.`id` = <%-escapedId%>;