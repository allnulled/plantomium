<%
/**
 * 
 * ----
 * 
 * ### `/src/...`
 * 
 * @name `unnamed template`
 * @type [EJS template]
 * @has 
 * @uses 
 * @modifies 
 * @receives 
 * @returns 
 * @throws 
 * @description 
 * 
 */


const new_session_token = sqlString.escape(parameters.new_session_token);
const new_refresh_token = sqlString.escape(parameters.new_refresh_token);
const refresh_token = sqlString.escape(parameters.refresh_token);

%>
UPDATE sessions
  SET sessions.refresh_token = <%-new_refresh_token%>,
    sessions.session_token = <%-new_session_token%>
  WHERE refresh_token = <%-refresh_token%>;