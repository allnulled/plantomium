<%
/**
 * 
 * ----
 * 
 * ### `/src/...`
 * 
 * @name `unnamed template`
 * @type [EJS template]
 * @has 
 * @uses 
 * @modifies 
 * @receives 
 * @returns 
 * @throws 
 * @description 
 * 
 */

	
cms.utils.trace("cms.auth.queries.selectUserById");
const path = require("path");
const id = sqlString.escape(parameters.id);
const fields = cms.utils.toSelectFieldsSql(0, cms.originalSchema.constraints.users.attributes, "users");

%>
SELECT <%-fields%>
  FROM users
  WHERE
    users.id = <%-id%>