<%
/**
 * 
 * ----
 * 
 * ### `/src/...`
 * 
 * @name `unnamed template`
 * @type [EJS template]
 * @has 
 * @uses 
 * @modifies 
 * @receives 
 * @returns 
 * @throws 
 * @description 
 * 
 */

	
const path = require("path");
const id = sqlString.escape(parameters.id);
const fields = cms.utils.toSelectFieldsSql(0, Object.keys(cms.originalSchema.columns.users), "users");

%>
SELECT <%-fields%>
  FROM users
  WHERE
    users.id = <%-id%>