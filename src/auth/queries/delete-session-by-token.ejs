<%
/**
 * 
 * ----
 * 
 * ### `/src/...`
 * 
 * @name `unnamed template`
 * @type [EJS template]
 * @has 
 * @uses 
 * @modifies 
 * @receives 
 * @returns 
 * @throws 
 * @description 
 * 
 */


cms.utils.trace("cms.auth.queries.deleteSessionByToken");
const session_token = sqlString.escape(parameters.session_token);

%>
DELETE
  FROM sessions 
  WHERE sessions.session_token = <%-session_token%>