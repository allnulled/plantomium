<%
/**
 * 
 * ----
 * 
 * ### `/src/...`
 * 
 * @name `unnamed template`
 * @type [EJS template]
 * @has 
 * @uses 
 * @modifies 
 * @receives 
 * @returns 
 * @throws 
 * @description 
 * 
 */


const recovery_token = sqlString.escape(parameters.recovery_token);
const user_id = sqlString.escape(parameters.user_id);
const password = sqlString.escape(parameters.password);

%>

UPDATE users
  SET users.recovery_token = <%-recovery_token%>,
    users.password = <%-password%>
  WHERE users.id = <%-user_id%>