<%
/**
 * 
 * ----
 * 
 * ### `/src/...`
 * 
 * @name `unnamed template`
 * @type [EJS template]
 * @has 
 * @uses 
 * @modifies 
 * @receives 
 * @returns 
 * @throws 
 * @description 
 * 
 */

cms.utils.trace("cms.auth.queries.insertSessionByUser");
const fields = cms.utils.toInsertFieldsSql(0, Object.keys(parameters));
const values = cms.utils.toInsertValuesSql(0, Object.values(parameters));
%>
INSERT INTO sessions (<%-fields%>
) VALUES (<%-values%>
);