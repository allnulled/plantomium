<%
/**
 * 
 * ----
 * 
 * ### `/src/...`
 * 
 * @name `unnamed template`
 * @type [EJS template]
 * @has 
 * @uses 
 * @modifies 
 * @receives 
 * @returns 
 * @throws 
 * @description 
 * 
 */

cms.utils.trace("cms.auth.queries.selectUserByNameOrEmail");
const path = require("path");
const userFields = cms.utils.toSelectFieldsSql(0, cms.originalSchema.constraints.users.attributes, "users");
const { name, email } = {
  name: sqlString.escape(parameters.name),
  email: sqlString.escape(parameters.email),
};
  
%>
SELECT <%-userFields%>
  FROM users
  WHERE (
    users.name = <%-name%>
    OR users.email = <%-email%>
  );