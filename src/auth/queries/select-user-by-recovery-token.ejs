<%
/**
 * 
 * ----
 * 
 * ### `/src/...`
 * 
 * @name `unnamed template`
 * @type [EJS template]
 * @has 
 * @uses 
 * @modifies 
 * @receives 
 * @returns 
 * @throws 
 * @description 
 * 
 */


const recovery_token = sqlString.escape(parameters.recovery_token);
const fields = cms.utils.toSelectFieldsSql(0, cms.originalSchema.constraints.users.attributes, "users");

%>
SELECT <%-fields%>
  FROM users
  WHERE users.recovery_token = <%-recovery_token%>;