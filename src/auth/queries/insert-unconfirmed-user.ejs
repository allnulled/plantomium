<%
const fields = cms.utils.toInsertFieldsSql(Object.keys(parameters));
const values = cms.utils.toInsertValuesSql(Object.values(parameters));
%>
INSERT INTO unconfirmed_users (<%-fields;%>
) VALUES (<%-values;%>
);