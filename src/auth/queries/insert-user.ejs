<%
/**
 * 
 * ----
 * 
 * ### `/src/...`
 * 
 * @name `unnamed template`
 * @type [EJS template]
 * @has 
 * @uses 
 * @modifies 
 * @receives 
 * @returns 
 * @throws 
 * @description 
 * 
 */

const fieldsList = Object.keys(parameters);
const fields = cms.utils.toInsertFieldsSql(0, fieldsList);
const values = cms.utils.toInsertValuesSql(0, Object.values(parameters), fieldsList);
%>
INSERT INTO users (<%-fields%>
) VALUES (<%-values%>
);