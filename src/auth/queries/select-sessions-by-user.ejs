<%
/**
 * 
 * ----
 * 
 * ### `/src/...`
 * 
 * @name `unnamed template`
 * @type [EJS template]
 * @has 
 * @uses 
 * @modifies 
 * @receives 
 * @returns 
 * @throws 
 * @description 
 * 
 */

cms.utils.trace("cms.auth.queries.selectSessionsByUser");
const { id_user, sessionFields } = {
  id_user: sqlString.escape(parameters.id_user),
  sessionFields: cms.utils.toSelectFieldsSql(0, cms.originalSchema.constraints.sessions.attributes, "sessions"),
};
  
%>
SELECT <%-sessionFields%> 
  FROM sessions
  WHERE sessions.id_user = <%-id_user%>;