/**
 * 
 * ----
 * 
 * ### `/src/auth/middlewares/only-users.js`
 * 
 * @name `onlyUsers`
 * @type 
 * @has 
 * @uses 
 * @modifies 
 * @receives 
 * @returns 
 * @throws 
 * @description 
 * 
 */
module.exports = (...whereUsers) => async function(request, response, next) {
	response.json({ok:"ok"})
};