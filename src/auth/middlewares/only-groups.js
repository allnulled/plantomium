/**
 * 
 * ----
 * 
 * ### `/src/auth/middlewares/only-groups.js`
 * 
 * @name `onlyGroups`
 * @type 
 * @has 
 * @uses 
 * @modifies 
 * @receives 
 * @returns 
 * @throws 
 * @description 
 * 
 */
module.exports = (...whereGroups) => async function(request, response, next) {
	response.json({ok:"ok"})
};