<%
/**
 * 
 * ----
 * 
 * ### `/src/...`
 * 
 * @name `unnamed template`
 * @type [EJS template]
 * @has 
 * @uses 
 * @modifies 
 * @receives 
 * @returns 
 * @throws 
 * @description 
 * 
 */

const table = sqlString.escapeId(actorClass.Table);
const {
  fields = undefined,
  id = undefined,
  join = [],
  limit = 20,
  sort = undefined,
  page = 1,
} = parameters.input;
const fieldsFinal = cms.utils.toSelectFieldsSql(undefined, actorClass.Table, false);
const whereFinal = cms.utils.toSelectWhereSql([[actorClass.Table + ".id","=",id]], false, actorClass.Table);
//const joinFinal = cms.utils.toSelectJoinSql(join, actorClass.Table);
const orderFinal = cms.utils.toSelectOrderSql(sort);
const limitFinal = cms.utils.toSelectLimitSql(limit);
const offsetFinal = cms.utils.toSelectOffsetSql(limit, page);


%>
# select one:
SELECT 
  <%-fieldsFinal%>
  FROM <%-table%>
  <%-whereFinal ? ("WHERE " + whereFinal) : "# no WHERE..." %><%/* joinFinal ? joinFinal : "  # no JOIN..." */%>
  <%-orderFinal ? ("ORDER BY " + orderFinal) : "# no ORDER BY..." %>
  <%-limitFinal ? ("LIMIT " + limitFinal) : "# no LIMIT..." %>
  <%-offsetFinal ? ("OFFSET " + offsetFinal) : "# no OFFSET..." %>
;