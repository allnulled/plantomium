<%
/**
 * 
 * ----
 * 
 * ### `/src/...`
 * 
 * @name `unnamed template`
 * @type [EJS template]
 * @has 
 * @uses 
 * @modifies 
 * @receives 
 * @returns 
 * @throws 
 * @description 
 * 
 */

const table = sqlString.escapeId(actorClass.Table);
const {
  where,
  data
} = parameters.input;
const whereFinal = cms.utils.toSelectWhereSql(where, false, actorClass.Table, true);
const dataFinal = cms.utils.toUpdateValuesSql(data);
if(dataFinal.length === 0) {
	throw new Error("Required at least 1 item in <data> on <cms.rest.queries.updateMany> [ERR:640]");
}
if(whereFinal.length === 0) {
	throw new Error("Required at least 1 item in <where> on <cms.rest.queries.updateMany> [ERR:640]")
}
%>
# update many:
UPDATE <%-table%>
SET <%-dataFinal%>
WHERE <%-whereFinal%>;