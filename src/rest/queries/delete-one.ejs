<%
/**
 * 
 * ----
 * 
 * ### `/src/...`
 * 
 * @name `unnamed template`
 * @type [EJS template]
 * @has 
 * @uses 
 * @modifies 
 * @receives 
 * @returns 
 * @throws 
 * @description 
 * 
 */

const table = sqlString.escapeId(actorClass.Table);
const {
  id,
  where = [],
} = parameters.input;
const whereFinal = cms.utils.toSelectWhereSql(0, [[ "id", "=", id]], false, actorClass.Table, true);
%>
# delete one:
DELETE FROM <%-table%>
WHERE <%-whereFinal%>;