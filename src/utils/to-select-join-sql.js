const sqlString = require("sqlstring");

/**
 * 
 * ----
 * 
 * ### `/src/utils/to-select-join-sql.js`
 * 
 * @name `toSelectJoinSql`
 * @type 
 * @has 
 * @uses 
 * @modifies 
 * @receives 
 * @returns 
 * @throws 
 * @description 
 * 
 */
module.exports = function(selectJoin = []) {
	let sql = "";
	// @TODO:
	return sql;
}